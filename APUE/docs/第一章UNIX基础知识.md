### 1.输入和输出
* 文件描述符：一个非负整数，内核用此来标识一个特定进程正在访问的文件
* 标准输入：文件描述符默认为0，STDIN_FILENO
* 标准输出：文件描述符默认为1,STDOUT_FILENO
* 标准错误：文件描述符默认为2,STDERR_FILENO

### 2.出错处理
* errno：出错时通常会被设置为具有特定信息的值，具体可以`man 3 errno`
* errno应注意的规则：
1. 如果没有出错，其值不会被进程清除
2. 任何函数都不会将errno设置为0
* 常用的出错函数：strerror和perror(这两个函数都基于errno的值报错)
